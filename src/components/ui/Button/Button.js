import React from 'react';
import 'Button.css'

const Button = (props) => { 
  return (
    <div className="Button" data-testid="Button">
      Button Component
    </div>
  )
};

export default Button;
